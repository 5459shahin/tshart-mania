import React from 'react';

const Uncle = ({house}) => {
    return (
        <div>
            <h2>my uncle</h2>
            <p><small>house</small>{house}</p>
        </div>
    );
};

export default Uncle;